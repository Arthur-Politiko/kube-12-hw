apiVersion: v1
kind: Pod
metadata:
  name: netology-web
  namespace: test
  labels:
    app: netology-web
    version: v2
spec:
  containers:
    - name: mycontainer
      image: gcr.io/kubernetes-e2e-test-images/echoserver:2.2
      ports:
        - containerPort: 8080